---
module: {{ module_name }}
old_version: "{{ old_version }}"
new_version: "{{ new_version }}"
maturity: beta
last_verified: "{{ timestamp }}"
breaking_changes: {{ breaking_changes_count }}
deprecated_apis: {{ deprecated_count }}
new_apis: {{ new_apis_count }}
---

# {{ module_name }} - API Changes ({{ old_version }} → {{ new_version }})

## Summary

| Category | Count |
|----------|-------|
| Breaking Changes | {{ breaking_changes_count }} |
| New Deprecations | {{ new_deprecations_count }} |
| Signature Changes | {{ signature_changes_count }} |
| Removed APIs | {{ removed_apis_count }} |
| Added Files | {{ added_files_count }} |
| Removed Files | {{ removed_files_count }} |

---

## Breaking Changes

{% if breaking_changes %}
{% for change in breaking_changes %}
### {{ change.name }}

- **Type**: {{ change.type }}
- **File**: `{{ change.file }}`
{% if change.old_signature %}
- **이전 ({{ old_version }})**: `{{ change.old_signature }}`
{% endif %}
{% if change.new_signature %}
- **현재 ({{ new_version }})**: `{{ change.new_signature }}`
{% endif %}

**마이그레이션 가이드**:
{{ change.migration_guide | default("마이그레이션 가이드 작성 필요") }}

{% endfor %}
{% else %}
Breaking changes가 발견되지 않았습니다.
{% endif %}

---

## Deprecated APIs

{% if deprecated_apis %}
| API | 버전 | 대체 API | 메시지 |
|-----|------|---------|--------|
{% for api in deprecated_apis %}
| `{{ api.name }}` | {{ api.version | default("N/A") }} | {{ api.replacement | default("-") }} | {{ api.message | default("-") }} |
{% endfor %}
{% else %}
Deprecated API가 발견되지 않았습니다.
{% endif %}

---

## New Deprecations in {{ new_version }}

이전 버전({{ old_version }})에서는 정상이었으나 {{ new_version }}에서 새로 deprecated된 API:

{% if new_deprecations %}
{% for api in new_deprecations %}
### {{ api.name }}

- **파일**: `{{ api.file }}:{{ api.line }}`
- **메시지**: {{ api.message }}
{% if api.replacement %}
- **대체 API**: `{{ api.replacement }}`
{% endif %}

{% endfor %}
{% else %}
새로운 deprecation이 없습니다.
{% endif %}

---

## Signature Changes

함수 시그니처가 변경된 API:

{% if signature_changes %}
{% for change in signature_changes %}
### {{ change.function_name }}

- **파일**: `{{ change.file }}`
- **변경 유형**: {{ change.change_type }}

**이전 ({{ old_version }})**:
```cpp
{{ change.old_signature }}
```

**현재 ({{ new_version }})**:
```cpp
{{ change.new_signature }}
```

{% endfor %}
{% else %}
시그니처 변경이 없습니다.
{% endif %}

---

## Removed APIs

{{ old_version }}에 존재했으나 {{ new_version }}에서 제거된 API:

{% if removed_apis %}
| API | 타입 | 마지막 파일 |
|-----|------|-------------|
{% for api in removed_apis %}
| `{{ api.name }}` | {{ api.type }} | `{{ api.file }}` |
{% endfor %}
{% else %}
제거된 API가 없습니다.
{% endif %}

---

## File Changes

### Added Files ({{ added_files | length }})

{% if added_files %}
{% for file in added_files[:20] %}
- `{{ file }}`
{% endfor %}
{% if added_files | length > 20 %}
... 그리고 {{ added_files | length - 20 }}개 더
{% endif %}
{% else %}
추가된 파일 없음
{% endif %}

### Removed Files ({{ removed_files | length }})

{% if removed_files %}
{% for file in removed_files[:20] %}
- `{{ file }}`
{% endfor %}
{% if removed_files | length > 20 %}
... 그리고 {{ removed_files | length - 20 }}개 더
{% endif %}
{% else %}
제거된 파일 없음
{% endif %}

---

## Migration Checklist

{{ new_version }}으로 마이그레이션 시 확인 사항:

{% if breaking_changes %}
- [ ] Breaking changes 대응 완료 ({{ breaking_changes | length }}개)
{% endif %}
{% if deprecated_apis %}
- [ ] Deprecated API 대체 완료 ({{ deprecated_apis | length }}개)
{% endif %}
{% if signature_changes %}
- [ ] 시그니처 변경 대응 완료 ({{ signature_changes | length }}개)
{% endif %}
{% if removed_apis %}
- [ ] 제거된 API 대체 완료 ({{ removed_apis | length }}개)
{% endif %}
- [ ] 컴파일 테스트 완료
- [ ] 런타임 테스트 완료

---

*Generated by NarshaMCP Engine Documentation System*
*Issue: #2836*
